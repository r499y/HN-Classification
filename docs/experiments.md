# Experiments

## Planned canonical experiment families
- CLAM + Lunit baseline
- TransMIL + CONCH vanilla
- TransMIL + CONCH k-bags + penalized selection (final)

## Current state
- Raw experimentation logic is primarily in notebooks (`notebooks/`).
- Modular training/evaluation stubs are in `src/training` and `src/eval` awaiting import of missing local project files.
